/**
 * La foto de perfil, en un solo sitio.
 *
 * Sale en el sidebar, en Acerca, en la firma de cada entrada y en las imágenes
 * que se dibujan al compilar (tarjeta al compartir y láminas del carrusel).
 * Al cambiarla, esto es lo único que hay que tocar.
 *
 * La ruta es relativa a la raíz del sitio: las páginas le anteponen `BASE_URL`
 * y lo que se dibuja con satori, la carpeta `public/`.
 */
export const FOTO_PERFIL = 'assets/img/tonny.jpg';
